@model IEnumerable<Library.WebApplication.Models.ArtistViewModel>
@{
    ViewBag.Title = "Autorzy";
}

<div class="btn-group mt-10" role="group">
    <button type="button" onclick="location.href='@Url.Action("Add", "Artist")'" class="btn btn-success btn-lg" aria-haspopup="true" aria-expanded="false">
        @Html.Name("Dodaj autora")
        <span class="glyphicon glyphicon-plus" style="padding-left:10px;"></span>
    </button>
</div>

<div class="row mt-20">
    <div class="col-sm-6">
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Surname)
                </th>
                <th></th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Surname)
                    </td>
                    <td>
                        @using (Html.BeginForm("Edit", "Artist", new { id = item.Id }, FormMethod.Get, new { }))
                        {
                            <input type="submit" value="Edytuj" class="btn btn-default" />
                        }
                    </td>
                    <td>
                        @using (Html.BeginForm("Delete", "Artist", new { id = item.Id }, FormMethod.Post, new { }))
                        {
                            <input type="submit" value="Usuń" class="btn btn-danger" />
                        }
                    </td>
                </tr>
            }
        </table>
    </div>
</div>
